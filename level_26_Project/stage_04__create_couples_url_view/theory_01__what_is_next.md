## Что дальше?

"Мозг" проекты мы уже сделали. 
Осталось создать скелет (url-view-template)  
и нарастить "мышцы" (формы и полный функционал view).

1. Прежде все, необходимо подобрать HTML-шаблоны под весь функционал проекта (выполнено в `draft_template`).

2. Далее, создаём скелет проекта:
    * Ссылку на главную + вью + шаблон
    * И далее, все ссылки из главной должны открывать вспомогательные страницы 
    * (пока без функционала на этих страницах)
    * Главное, чтобы все шаблоны правильно отражали статику и картинки.

3. Для выполнения этой задачи:
   * Создаём тесты
   * Далее создаём маршруты-вью-шаблоны под эти тесты
   * И так до тех пор, пока не пройдут все тесты

    
---

### 2. Где логичнее разместить эти тесты

Так как тест проверяет **все URL проекта** (и `users`, и `shop`, и `admin`), — он не принадлежит   
какому-то одному приложению. Это — **интеграционный** тест.  

Поэтому **идеальное место — отдельный модуль в корне**, например:

```
project_root/
├── manage.py
├── conftest.py
├── users/
│   └── tests/
│       └── ...
├── shop/
│   └── tests/
│       └── ...
└── tests/
    ├── __init__.py
    ├── test_url_view_template.py   👈 сюда!
```

**Плюсы такого размещения:**

* Логически правильно: 
  * этот тест — интеграционный (проектный), 
  * а не юнит-тест конкретного приложения.
* `pytest` автоматически найдёт его, так как в корне уже есть `conftest.py`.
* Дополнительно, это позволит избежать дублирования URL-тестов внутри отдельных приложений.

